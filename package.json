{
  "name": "equipment-campaign-site",
  "version": "1.0.0",
  "private": true,
  "description": "Equipment campaign page with admin-controlled text, images, colors, and 1-20 item cards.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "docker:build": "docker build -t equipment-campaign-site .",
    "docker:run": "docker run --rm -p 3000:3000 -v equipment-campaign-data:/app/data equipment-campaign-site"
  },
  "engines": {
    "node": ">=18"
  }
}
